name: Pipeline - Promotion Manager

on:
  workflow_dispatch:
    inputs:
      target_env:
        description: "Entorno de destino (staging o production)"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production

permissions:
  contents: write

jobs:
  promote-to-staging:
    if: github.event.inputs.target_env == 'staging'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: staging
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "./client/package-lock.json"

      - name: Merge Develop -> Staging
        run: |
          echo "🚀 Promocionando Develop a Staging..."
          git fetch origin develop
          git merge origin/develop --no-ff -m "chore: promote develop to staging [modificado por antigravity]"
          git push origin staging

      - name: Trigger Easypanel Staging
        run: |
          echo "📡 Activando Webhook API Staging..."
          curl -f -X POST "${{ secrets.EASYPANEL_API_WEBHOOK }}" || echo "⚠️ Webhook API falló (continuando workflow)"

  promote-to-production:
    if: github.event.inputs.target_env == 'production'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "./client/package-lock.json"

      - name: Merge Staging -> Master
        run: |
          echo "🚀 Promocionando Staging a Producción (Master)..."
          git fetch origin staging
          git merge origin/staging --no-ff -m "release: promote staging to production [modificado por antigravity]"
          git push origin master

      - name: Trigger Easypanel Production
        run: |
          echo "📡 Activando Webhooks de Producción..."
          if [ -n "${{ secrets.EASYPANEL_API_WEBHOOK }}" ]; then
            curl -f -X POST "${{ secrets.EASYPANEL_API_WEBHOOK }}" || echo "⚠️ Webhook API falló"
          else
            echo "⚠️ Secret EASYPANEL_API_WEBHOOK no definido"
          fi

          if [ -n "${{ secrets.EASYPANEL_FRONTEND_WEBHOOK }}" ]; then
            curl -f -X POST "${{ secrets.EASYPANEL_FRONTEND_WEBHOOK }}" || echo "⚠️ Webhook Frontend falló"
          else
             echo "⚠️ Secret EASYPANEL_FRONTEND_WEBHOOK no definido"
          fi
